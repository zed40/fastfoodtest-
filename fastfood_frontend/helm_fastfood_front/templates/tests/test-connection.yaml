apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm_fastfood_front.fullname" . }}-test-connection"
  labels:
    {{- include "helm_fastfood_front.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm_fastfood_front.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
